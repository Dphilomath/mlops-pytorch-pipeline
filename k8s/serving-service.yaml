apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: ml-training
spec:
  selector:
    app: model-serving
  ports:
  - protocol: TCP
    port: 80        # Service port exposed inside the cluster
    targetPort: 8080 # Container port
  type: ClusterIP
