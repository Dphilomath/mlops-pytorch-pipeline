apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: ml-training
spec:
  selector:
    app: model-serving
  ports:
  - protocol: TCP
    port: 9876
    targetPort: 9876
  type: ClusterIP
