apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: ml-training
spec:
  selector:
    app: model-serving
  ports:
  - protocol: TCP
    port: 9876
    targetPort: 9876
    nodePort: 30000
  type: NodePort
