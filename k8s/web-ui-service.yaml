apiVersion: v1
kind: Service
metadata:
  name: web-ui
  namespace: ml-training
  labels:
    project: mlops-pytorch-pipeline
spec:
  type: ClusterIP
  selector:
    app: web-ui
    project: mlops-pytorch-pipeline
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
